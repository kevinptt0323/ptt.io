import React, { PureComponent } from 'react';

import Typography from 'material-ui/Typography'

class Favorite extends PureComponent {
  render() {
    return (
      <Typography type="title"> favorite </Typography>
    );
  }
};

export default Favorite;
